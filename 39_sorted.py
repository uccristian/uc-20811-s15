# función que devuelve una lista ordenada (de menor a mayor) a partir de cualquier colección

numeros = {5, 2, 9, 1}

ordenados = sorted(numeros)
print(ordenados)

ordenados = sorted(numeros, reverse=True)
print(ordenados)
